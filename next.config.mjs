/** @type {import('next').NextConfig} */
const nextConfig = { reactStrictMode: true, output: 'export', images: { unoptimized: true }, basePath: '/a-table-etiquette', assetPrefix: '/a-table-etiquette/' };
export default nextConfig;

